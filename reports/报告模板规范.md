# API 平台探测报告 — 统一模板规范

> 本文档定义所有平台探测报告的**统一内容结构与样式**。以后每个平台生成的报告都必须遵循此模板。
> 文档分两部分：**Part 1 规范说明**（设计约定）+ **Part 2 完整样例**（用真实数据渲染的整份报告）。

---

# Part 1：模板规范说明

## 1.1 章节结构总表

报告固定按以下顺序组织。重点信息（执行摘要、推荐模型）置前，参考性信息（符号说明）置于附录。

| 顺序 | 章节 | 用途 | 数据来源 |
|:---:|------|------|---------|
| 0 | 📊 执行摘要 | 仪表盘式速览，一眼抓重点 | 各阶段汇总 |
| 1 | 一、平台概览 | 平台基础信息（含评分与公式说明） | Stage0 连通性 / Stage1 格式 / Stage2 端点 |
| 2 | 二、模型可用性总览 | 可用/不可用分布 + 各状态模型清单 | Stage2.5 预检 |
| 3 | 三、推荐模型 | Top-N 优选 | Stage3 能力探测 |
| 4 | 四、可用模型能力矩阵 | 可用模型 × 8 能力 | Stage3 能力探测 |
| 5 | 五、可用模型问题统计 | **按能力**分组，每个能力下再按错误类型聚合失败 | Stage3 能力探测 |
| 6 | 六、不可用模型分析 | **按模型**逐个列出不可用成因 | Stage2.5 预检 |
| 7 | 七、边界探测 | 上下文/输出/速率上限（per-model） | Stage4（`--mode deep`） |
| 8 | 八、稳定性分析 | 多次采样的成功率/延迟（per-model + 平台汇总） | Stage5（`--mode deep`） |
| 9 | 九、阶段失败记录 | 阶段级异常（条件显示） | failures |
| A | 附录 A：状态符号说明 | 能力说明/符号/阈值/错误分类参考 | 静态参考 |

## 1.2 关键数据来源说明（重要）

报告中有两处"问题/不可用"分析，**数据来源不同、互补、不重叠**，务必区分：

- **第五章「可用模型问题统计」** → 数据来自 **Stage3 能力探测**，只针对**可用模型**
  （已通过 Stage2.5 预检、基础对话能跑通的模型）。**按能力探针分组**（基础对话、流式、工具调用…），
  每个能力下再以 `不可用原因（UnavailableReason）+ 错误类型（ErrorCategory）` 聚合失败，
  展示该能力下出了什么问题、影响哪些模型。无失败的能力不出现在此章。
- **第六章「不可用模型分析」** → 数据来自 **Stage2.5 可用性预检**，针对**不可用模型**
  （连基础对话都跑不通的模型）。按**模型维度**逐个展开成因。

> 范围佐证：`Stage3._get_models_to_test()` 只测 Stage2.5 筛出的 `available_models`，
> 故能力矩阵与问题统计天然只覆盖可用模型；不可用模型不会进入 Stage3。

## 1.3 图表化约定

所有图表用纯文本/emoji 实现，确保终端、Markdown 阅读器、未来 UI 均可渲染：

| 元素 | 写法 | 用途 |
|------|------|------|
| 进度条 | `████████░░░░░░░░` | 可用率、能力支持率（满格 24 字符） |
| 状态点 | 🟢 健康 / 🟡 中等 / 🔴 差 | 健康度、可用性状态 |
| 可达标记 | ✅ 可达 / ❌ 不可达 | 连通性 |
| 星级 | ★★★★☆ | 稳定性评级（5 星制） |
| — | — | （能力点阵已废弃，由「支持能力详情」文字描述代替） |
| 排名徽章 | 🥇🥈🥉 | 推荐模型 Top-3 |

## 1.4 平台健康度判定规则（执行摘要用）

```
不可达（Stage0 reachable=False）         → 🔴 异常
可达，可用率 ≥ 80%                       → 🟢 良好
可达，可用率 40% ~ 80%                   → 🟡 中等
可达，可用率 < 40%                       → 🔴 差
```

## 1.5 能力矩阵单元符号

| 符号 | 含义 |
|:---:|------|
| ✓ | 支持且首字节健康（<30s） |
| 🟡 | 支持但响应缓慢（30~60s 或流式 fallback） |
| ⚠ | 支持但格式/质量有瑕疵（中可靠） |
| ? | 支持但可靠性低（语义可疑） |
| ✗ | 不支持 / 探测失败 |
| N/A | 未测试 |

> 能力矩阵**只展示 Stage2.5 判定可用的模型**，避免与可用清单矛盾。

---

# Part 2：完整样例报告

> 下面是新模板渲染的实际效果，使用 **OpenCodeZen 真实探测数据**。
> Stage4（边界探测）、Stage5（稳定性分析）当前未实现，相应章节用 `🔧 模拟示例` 标注，
> 展示将来实现后的样式。

---

# OpenCodeZen 探测报告

**生成时间**：2026-06-11 11:56:40 ｜ **探测模式**：standard ｜ **预检耗时**：4.4s

## 📊 执行摘要

| 平台健康度 | 模型可用率 | 可用 / 总数 | 推荐首选 |
|:---:|:---:|:---:|:---:|
| 🟡 **中等** | **66.7%** | 4 / 6 | `nemotron-3-ultra-free` |

**可用性分布**

```
完全可用  ████████████████░░░░░░░░  4  (66.7%)
不可用    ████████░░░░░░░░░░░░░░░░  2  (33.3%)
```

**一句话结论**：平台连通正常（OpenAI 格式，置信度 1.00），6 个模型中 4 个可用；
2 个因认证失败不可用；首选 `nemotron-3-ultra-free`（支持 6/8 能力）。

## 一、平台概览

| 项目 | 内容 |
|------|------|
| 平台名称 | OpenCodeZen |
| Base URL | `https://opencode.ai/zen/v1` |
| 综合评分 | **45.2 分** / 100 |
| API 格式 | `openai_chat_completions`（置信度 1.00） |
| 可达性 | ✅ 可达（2310 ms） |
| 模型总数 | 6（发现方式：api） |
| 端点 | `chat` `/chat/completions`、`models` `/models` |
| 可用性预检耗时 | 4.40 秒 |

> 💡 **综合评分计算公式说明**：
> 综合评分（百分制） = `基础可用分 (40%)` + `性能时延分 (30%)` + `功能覆盖分 (30%)`
> 1. **基础可用分**：`模型可用率 * 100`。
> 2. **性能时延分**：根据推荐首选模型时延计算（≤1000ms 得 100分，1000~3000ms 线性折算扣分，3000~10000ms 得 40分，其余得 0分）。
> 3. **功能覆盖分**：所有可用模型对“工具调用、视觉、JSON、推理、联网、多轮” 6 项高级能力支持率的平均得分。
> *若平台不可达或无任何可用模型，系统将触发短路熔断，最终判定为 0 分。*

## 二、模型可用性总览

| 状态 | 数量 | 占比 | 分布 | 模型列表 |
|------|:---:|:---:|------|------|
| 🟢 完全可用 | 4 | 66.7% | `████████████████░░░░░░░░` | `nemotron-3-ultra-free`<br>`deepseek-v4-flash-free`<br>`mimo-v2.5-free`<br>`north-mini-code-free` |
| 🔴 不可用 | 2 | 33.3% | `████████░░░░░░░░░░░░░░░░` | `qwen3.6-plus-free`<br>`minimax-m3-free` |

## 三、推荐模型

基于「能力覆盖数 → 高可靠探针数 → 基础对话延迟」综合排序：

| 排名 | 模型 | 支持能力数 | 支持能力详情 | 高可靠 | 基础对话延迟 |
|:---:|------|:---:|------|:---:|:---:|
| 🥇 | `nemotron-3-ultra-free` | 6/8 | 流式、非流式、工具调用、JSON、推理、多轮 | 6 | 6256 ms |
| 🥈 | `north-mini-code-free` | 4/8 | 流式、非流式、推理、多轮 | 4 | 1777 ms |
| 🥉 | `mimo-v2.5-free` | 4/8 | 流式、非流式、工具调用、JSON | 4 | 5560 ms |
> 注：`deepseek-v4-flash-free` 虽支持 6/8，但本轮基础对话探测失败，不纳入推荐。

## 四、可用模型能力矩阵

> 数据来源：Stage3 能力探测。**本表仅含 Stage2.5 判定可用的模型**。各能力含义见附录 A.0。

| 模型 | 流式 | 非流式 | 工具调用 | 视觉 | JSON | 推理 | 联网 | 多轮 | 支持数 |
|------|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|:---:|
| `nemotron-3-ultra-free` | ✓ | ✓ | ✓ | ✗ | ✓ | ✓ | ✗ | ✓ | **6/8** |
| `deepseek-v4-flash-free` | ✓ | ✗ | ✓ | ✗ | ✓ | ✓ | ✗ | ✓ | **6/8** |
| `north-mini-code-free` | ✓ | ✓ | ✗ | ✗ | ✗ | ✓ | ✗ | ✓ | **4/8** |
| `mimo-v2.5-free` | ✓ | ✓ | ✓ | ✗ | ✓ | ✗ | ✗ | ✗ | **4/8** |

**各能力支持率**（4 个可用模型中支持数）

```
流式      ████████████████████████  4/4  100%
非流式    ██████████████████░░░░░░  3/4   75%
工具调用  ██████████████████░░░░░░  3/4   75%
JSON      ██████████████████░░░░░░  3/4   75%
推理      ██████████████████░░░░░░  3/4   75%
多轮      ██████████████████░░░░░░  3/4   75%
视觉      ░░░░░░░░░░░░░░░░░░░░░░░░  0/4    0%
联网      ░░░░░░░░░░░░░░░░░░░░░░░░  0/4    0%
```

## 五、可用模型问题统计

> 数据来源：Stage3 能力探测，**仅针对可用模型**——按能力分组，展示各能力下的失败分布。

### 视觉

| 不可用原因 | 错误类型 | 出错表现 | 平台原始响应/详情 | 问题成因 | 影响模型数 | 受影响模型 | 处理建议 |
|------|------|------|------|------|:---:|------|------|
| 响应异常 | 格式异常 | HTTP 400 + 拒绝 `image_url` | HTTP 400: Image input not supported by this model | 视觉探测发 `image_url` 被模型拒绝，模型不支持多模态输入 | 4 | `deepseek-v4-flash-free`<br>`mimo-v2.5-free`<br>`nemotron-3-ultra-free`<br>`north-mini-code-free` | 视觉探测不适用，可忽略 |

### 非流式

| 不可用原因 | 错误类型 | 出错表现 | 平台原始响应/详情 | 问题成因 | 影响模型数 | 受影响模型 | 处理建议 |
|------|------|------|------|------|:---:|------|------|
| 响应异常 | 空响应 | HTTP 200 + 空 body | HTTP 200: [Empty response body] | 后端瞬时返回空响应 | 1 | `deepseek-v4-flash-free` | 重试，持续则报告平台 |

> HTTP 列语义见附录 A.3 末尾。

## 六、不可用模型分析

> 数据来源：Stage2.5 可用性预检。逐个列出**基础对话级不可用**（连基础对话都跑不通）的模型及成因。

共 **2** 个模型不可用：

| 模型 | 状态 | 成因 | HTTP | 错误摘要 | 成因说明 | 建议 |
|------|:---:|------|:---:|------|------|------|
| `qwen3.6-plus-free` | 🔴 | 认证失败 | 401 | `Invalid API key` | API Key 无效或套餐未覆盖该模型 | 检查密钥、确认套餐含该模型 |
| `minimax-m3-free` | 🔴 | 认证失败 | 401 | `Invalid API key` | API Key 无效或套餐未覆盖该模型 | 检查密钥、确认套餐含该模型 |

**成因小结**：2 个不可用模型均为**认证失败（401）**，集中在同一类。鉴于其余 4 个模型用
同一 Key 可正常调用，判断并非密钥本身错误，而是该 Key 套餐不含这两个模型的访问权限。

## 七、边界探测

> 探测方法：max_tokens 从大到小试至报错取最大成功值；上下文长度填充 prompt 从小到大试至 400 错误；RPM 读响应头。
> 覆盖范围：推荐排名前 N 个可用模型（由 `defaults.deep_probe.top_n` 配置，默认 3）。

| 模型 | 最大输出 token | 最大上下文长度 | 速率限制 RPM |
|------|:---:|:---:|:---:|
| `nemotron-3-ultra-free` | 131,072 | 128,000 | _未获取_ |
| `north-mini-code-free` | 131,072 | 128,000 | _未获取_ |
| `deepseek-v4-flash-free` | 65,536 | 64,000 | _未获取_ |

## 八、稳定性分析

> 平台整体：成功率 **96.7%**，综合评级 ★★★★★（5/5）
> 每模型各发送 5 次基础对话请求。

| 模型 | 成功率 | 平均延迟 | 星级 | 错误模式 |
|------|:---:|:---:|:---:|------|
| `nemotron-3-ultra-free` | 100.0% | 4,100 ms | ★★★★★ | — |
| `north-mini-code-free` | 100.0% | 1,800 ms | ★★★★★ | — |
| `deepseek-v4-flash-free` | 80.0% | 9,200 ms | ★★★★☆ | `read_timeout` |

## 九、阶段失败记录

> 仅当存在阶段级异常时显示。本次探测无阶段失败。

---

## 附录 A：状态符号说明

### A.0 八项能力说明

| 能力 | 探针 | 检验内容 |
|------|------|------|
| 非流式 | basic_chat | 单轮问答（非流式）能否正常返回 |
| 流式 | streaming | SSE 流式输出是否支持 |
| 工具调用 | tool_calling | function calling / tools 参数 |
| 视觉 | vision | 图片输入（image_url / base64） |
| JSON | json_mode | 结构化 JSON 输出 |
| 推理 | reasoning | 推理 / 思维链能力 |
| 联网 | web_search | 联网搜索能力 |
| 多轮 | multi_turn | 多轮对话上下文记忆（能否记住前几轮内容） |

### A.1 响应健康度

| 状态 | 阈值 | 含义 | 建议 |
|------|------|------|------|
| 🟢 健康 | 首字节 < 30s | 模型快速响应 | 推荐 |
| 🟡 半死不活 | 30s ~ 60s | 后端排队/慢 | 仅离线/异步场景 |
| 🔴 不响应 | > 60s 或异常 | 后端死锁或上游断连 | 避免使用 |

### A.2 能力矩阵符号

| 符号 | 含义 |
|:---:|------|
| ✓ | 支持且首字节健康（<30s） |
| 🟡 | 支持但响应缓慢（30~60s 或流式 fallback） |
| ⚠ | 支持但格式/质量有瑕疵 |
| ? | 支持但可靠性低（语义可疑） |
| ✗ | 不支持 / 探测失败 |
| N/A | 未测试 |

### A.3 错误分类（13 类 ErrorCategory）

| 分类 | 表现 | 根因 | 处理建议 |
|------|------|------|------|
| 连接失败 | DNS 失败 / 端口拒绝 | base_url 错或平台已关停 | 检查 URL 与网络 |
| 读取超时 | 建连后无字节 | 后端死锁 / 上游死 | 重试或弃用 |
| 上游断连 | other_side_closed | 上游 provider 不可达 | 弃用模型 |
| 代理拦截 | Access denied | 代理软件关键词拦截 | 调整代理规则 |
| 认证失败 | HTTP 401 | API Key 错或过期 | 更新密钥 |
| 权限不足 | HTTP 403 | 账户无权访问 | 升级套餐或换模型 |
| 模型不存在 | HTTP 404 | 平台已下线 | 改用其它模型 |
| 配额耗尽 | HTTP 429 | RPM / 余额耗尽 | 等待重置 |
| 付费墙 | HTTP 402 | 需付费套餐 | 升级账户 |
| 空响应 | HTTP 200 + 空 body | 后端故障 | 重试 |
| 格式异常 | HTTP 200 + 非法 JSON/SSE | 后端格式不规范 | 检查平台兼容性 |
| 内容乱码 | 语义崩溃 | 模型权重 / KV cache 损坏 | 弃用模型 |
| 未知 | 未归类异常 | 需查看错误样本 | 联系平台支持 |

**HTTP 状态码语义**（第五/六章「HTTP」列含义）：

| HTTP | 含义 |
|:---:|------|
| 200 | 请求成功但响应内容异常（空 body / 解析失败） |
| 400 | 请求被拒（参数不被接受，如视觉模型拒收 `image_url`） |
| 401 / 403 / 404 / 429 / 402 | 认证/权限/不存在/限流/付费，见上方分类表 |
| — | 未发出 HTTP 请求（网络层错误，如超时、断连） |

> 响应阈值可通过 `ProbeTimeouts` 配置调整（当前：fast=30s, slow=60s）。
