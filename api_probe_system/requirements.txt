# API平台智能探测系统 — 依赖清单
# 对应：系统架构设计文档 §7.1 后端核心技术栈
# Python 3.11+（开发环境实测 3.14.3）

httpx>=0.27          # 异步 HTTP 客户端，支持 HTTP/2 + 连接池
pydantic>=2.0        # 数据校验，配置 Schema 落地
pyyaml>=6.0          # YAML 配置/画像读写
aiosqlite>=0.20      # 异步 SQLite 访问
cryptography>=42     # .env.enc 加解密（Fernet + PBKDF2HMAC）
keyring>=24          # OS Keychain 存取主密码
fastapi>=0.110       # 接口层（M3 引入）
uvicorn>=0.27        # ASGI 服务器（M3 引入）

# 测试
pytest>=8.0
pytest-asyncio>=0.23
